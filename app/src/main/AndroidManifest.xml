<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />

            <data android:scheme="mailto" />
        </intent>
    </queries>

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:name=".di.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:configChanges="screenSize|screenLayout|orientation|keyboardHidden"
        android:theme="@style/Theme.VideoPlayer"
        tools:targetApi="tiramisu">
        <activity
            android:name=".player.presentation.PlayerActivity"
            android:exported="true"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden"
            android:label="@string/title_activity_player"
            android:supportsPictureInPicture="true"
            android:theme="@style/Theme.VideoPlayer">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
                <data android:scheme="file" />
                <data android:scheme="content" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VideoPlayer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.presentation.routes.player.PlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity
            android:name=".settings.presentation.routes.playback.PlaybackActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity
            android:name=".settings.presentation.routes.about.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity
            android:name=".settings.presentation.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity
            android:name=".settings.presentation.routes.language.LanguageActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />
        <activity
            android:name=".settings.presentation.routes.theme.ThemeActivity"
            android:exported="false"
            android:theme="@style/Theme.VideoPlayer" />

        <service
            android:name=".core.domain.MediaPlayerService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:permission="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>